{
  "version": 2,
  "builds": [
    {
      "src": "/index.js",
      "use": "@now/node-server"
    }
  ],
  "routes": [
    { "src": "/test", "dest": "/index.js" },
    { "src": "/api/ajustes/(?<id>[^/]+)", "dest": "/index.js" }
  ]
}
